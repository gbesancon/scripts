if (!hasProperty('useMavenLocal'))
{
  repositories {
    maven {
      url "https://maven.pkg.github.com/gbesancon"
      credentials {
        username "gbesancon"
        password "$System.env.GBESANCON_GITHUB_AZURE_DEVOPS_PACKAGE_TOKEN"
      }
    }
  }
}