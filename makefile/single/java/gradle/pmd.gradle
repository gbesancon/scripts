if(rootProject.checkPmdEnabled == 'true') {
  apply plugin: 'pmd'

  pmd {
    ignoreFailures = true
  }

  tasks.withType(Pmd) {
    reports {
      xml.enabled = true
      html.enabled = true
    }
  }
}